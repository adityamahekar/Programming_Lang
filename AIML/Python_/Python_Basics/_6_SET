s = {1, 2, 2, 5, 3}                        # duplicates removed
r = {1, 3, 2, 7, 8, 9}

print(len(s))
print(s)

print(s.intersection(r))                   # common elements
print(s.union(r))                          # all elements
